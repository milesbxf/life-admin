---
description: Health and fitness dashboard — WHOOP recovery/sleep/HRV, Cronometer nutrition, biometrics
argument-hint: Optional scope (e.g. "weekly" for 7-day summary, default is today)
---

Spawn the `health-reporter` agent with scope: "$ARGUMENTS" (use "daily" if empty, "weekly" if user said weekly/week/7 days).

Display its output directly. No extra commentary needed.
